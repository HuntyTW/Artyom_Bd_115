#ifndef MERGEVSTAV_HPP
#define MERGEVSTAV_HPP

#include <iostream>
#include <vector>

using namespace std;

namespace biv {
	namespace sortings {
		void mergevstav(vector<int> &a, size_t start, size_t end);
	}
}

#endif